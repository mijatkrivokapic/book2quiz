package com.example.book2quiz.model;

public enum CharacteristicStatus {
    /** Awaiting review (the default for generated characteristics). */
    PENDING,
    /** Reviewed and accepted. */
    APPROVED
}
